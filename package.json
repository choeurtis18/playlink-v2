{
  "name": "playlink-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @playlink/shared run dev & pnpm --filter !@playlink/shared -r --parallel run dev",
    "build": "pnpm --filter @playlink/shared run build && pnpm --filter !@playlink/shared -r run build",
    "lint": "pnpm -r run lint",
    "type-check": "pnpm -r run type-check"
  },
  "devDependencies": {},
  "pnpm": {
    "overrides": {}
  }
}
